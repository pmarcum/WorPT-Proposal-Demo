%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%% FREQUENTLY USED formatting %%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%   Consolidates the formatting applied to our documents in a single file
%   Note that one-off changes are best put durectly in the document's preamble
%
%  To incorporate these formatting commands, in the preamble of your 
%  document, simply type:  
%  \usepackage{FUformatting}
% -------------------------------------------

% Set the page margins: 
\usepackage[top=1.0in, left=1in, right=1in, bottom=1.0in]{geometry}

% allow for wrapping long text strings inside a cell within a table
\newcolumntype{M}[1]{>{\begin{varwidth}[t]{#1}}l<{\end{varwidth}}}
\definecolor{mediumelectricblue}{rgb}{0.01, 0.31, 0.59}

% place a line at the top with title to the right and the program name to the left
\definecolor{WatermarkColor}{HTML}{8887AD}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}

\titleformat{\section}{\centering\color{NavyBlue}\normalfont\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\centering\color{NavyBlue}\normalfont\large\itshape}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\centering\color{NavyBlue}\normalfont\normalsize}{\thesubsubsection}{1em}{}

\interfootnotelinepenalty=10000
\crefformat{footnote}{#2\footnotemark[#1]#3}

\def\square{\vrule height 4.5pt width 4pt depth -0.5pt}
\def\threesquares{\square~\square~\square\ }

% suppress new paragraph indentation
\setlength\parindent{0pt}

% put any extra white space appearing on a page at the bottom of the page
\raggedbottom

\newtcolorbox{callout}[4][]
{colback=#3!5!white,colframe=#3!75!black,fonttitle=\bfseries,title={#4},#2,}

\makeatletter \def\expinput#1{\@@input#1} \makeatother

% make the references appear as "blah blah blah [1,3] instead of (1;3)
\setcitestyle{square}
\setcitestyle{citesep={,}}

% Controls the spacing between lines in the Table of Contents
\setlength\cftparskip{2pt}
\setlength\cftbeforesecskip{2pt}
\setlength\cftaftertoctitleskip{2pt}

\captionsetup[table]{font={stretch=1}}     %% change 1.2 as you like
\captionsetup[figure]{font={stretch=1}}    %% change 1.2 as you like

% Set up the spacing between paragraphs.  The first number below is the 
% preferred spacing, and the plus 1pt minus 2pt is the amount of flexibility
% given to the code to make the best use of the available space
\parskip =12pt plus 1pt minus 2pt

% make the vertical space between paragraphs smaller than default
%\addtolength{\parskip}{-5pt}
